# Secrets / local config
.env
config.yaml
docker-compose.yml
docker-compose.jellyfin.yml
jellyfin-config/
jellyfin-cache/

# Jellyfin backend manual-verification test rig (issue #24) — throwaway
# second bot/container, deleted after verification. Never shipped, never
# meant to reach git even transiently.
.env.jellyfin-test
config.yaml.jellyfin-test
docker-compose.jellyfin-test.yml
scratch-jellyfin-test/
cache-jellyfin-test/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Runtime data
*.db
*.sqlite3
/tmp/
/cache/
/scratch/

# Testing
.pytest_cache/

# Claude Code local settings/skills (installed tooling, personal hook config)
.claude/

# Internal dev docs — personal setup details / forensic build notes, not
# needed to install or run CineSnip. Kept locally for Claude Code context.
/CLAUDE.md
/DESIGN.md
/PRODUCT.md
/docs/

# AI-tooling working output for this project (design critiques, hook
# cache) — internal scratch artifacts, not user-facing docs and not
# needed to run CineSnip.
.impeccable/

# Local dev-only compose override (bind-mounts ./app for hot reload) —
# Compose auto-merges it, so it must never reach another installer's clone.
docker-compose.override.yml

# OS / editor cruft
.DS_Store
*.log
